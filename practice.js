const people = ["Kyle", "Aki", "Bryan", "Jerm", "Matt"]; 


for (let i = 0; i < people.length; i++) {
    console.log(people[i].toUpperCase());
}
/* This example is to show how to run a loop over an array */
/* adding a while loop*/
let count = 0;
while (count <10) {
    count++;
    console.log(count)
}
 /* while loop will keep running until it prints 10, as the loop is true until then */